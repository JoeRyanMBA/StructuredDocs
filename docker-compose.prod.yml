version: '3.8'

services:
  app:
    build: .
    container_name: structureddocs_app
    environment:
      - PORT=8080
      # Use absolute path for SQLite database (4 slashes: sqlite:// + //)
      - DATABASE_URL=sqlite:////opt/structureddocs/instance/structured_docs.db
      - ENABLE_BLUEPRINTS_FILE=.enable_blueprints
      - IMAGE_STORAGE_ROOT=/app/data/images
      # Email configuration (set these in your deployment environment)
      - EMAIL_PROVIDER=${EMAIL_PROVIDER:-sendgrid}
      - SENDGRID_API_KEY=${SENDGRID_API_KEY}
      - DEFAULT_FROM_EMAIL=${DEFAULT_FROM_EMAIL:-no-reply@structureddocs.online}
      - FROM_EMAIL=${FROM_EMAIL:-}
      - FROM_NAME=${FROM_NAME:-StructuredDocs}
      - SENDGRID_VERIFIED_SENDER=${SENDGRID_VERIFIED_SENDER:-}
      - FRONTEND_URL=${FRONTEND_URL:-https://structureddocs.online}
      - ADMIN_API_KEY=${ADMIN_API_KEY:-}
      - SECRET_KEY=${SECRET_KEY}
    ports:
      - "8080:8080"
    restart: unless-stopped
    # Persist SQLite DB and uploaded images across restarts
    volumes:
      - ./instance:/app/instance
      # CRITICAL: Mount persistent storage for images
      # Host permissions MUST be 777: chmod 777 ./data/images ./instance
      - ./data/images:/app/data/images
      - ./data/images:/app/backend/static/images
    command: ["./start.sh"]
