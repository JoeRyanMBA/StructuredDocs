{
  "name": "structureddocs",
  "version": "1.0.0",
  "description": "StructuredDocs application",
  "main": "start.sh",
  "scripts": {
    "build": "echo '🔧 Starting build process...' && cd frontend && npm install && npm run build && cd .. && echo '✅ Build completed - Python dependencies will be installed at runtime'",
    "start": "./start.sh",
    "dev": "npm --prefix frontend run dev",
    "postinstall": "echo '🔧 Postinstall completed - Python dependencies will be installed at runtime'"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
