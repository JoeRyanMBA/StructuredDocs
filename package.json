{
  "name": "structureddocs",
  "version": "1.0.0",
  "description": "StructuredDocs application",
  "main": "start.sh",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "./start.sh",
    "dev": "./start.sh"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
